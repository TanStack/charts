# Observable Plot snapshots

These files are byte-for-byte snapshots from
`observablehq/plot/test/data` at commit
`356f579b1d947ee05a914420eddff0f29cee300a`.

The generator records each file's upstream source and SHA-256 in
`@charts-poc/demo-data/metadata`. Observable Plot is MIT licensed; the
metadata retains the separate upstream dataset attribution.
